---
import BaseLayout from "~/layouts/BaseLayout.astro";
import Box from "~/components/ui/Box.astro";
import ContactForm from "~/components/contactForm/ContactForm.astro";

console.log(import.meta.env.SMTP_SECURE === "true");
console.log(import.meta.env.SMTP_SECURE);
console.log(import.meta.env.SMTP_SECURE === true);
---

<BaseLayout>
  <main class="min-h-screen flex items-center justify-center">
    <Box class="bg-theme-mist w-125 rounded-2.5 p-10">
      <ContactForm />
    </Box>
  </main>
</BaseLayout>

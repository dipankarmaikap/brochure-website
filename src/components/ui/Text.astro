---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"p"> {}

const { class: className, ...props } = Astro.props;
---

<p
  class:list={["paragraph text-theme-muted text-sm font-normal leading-5", className]}
  {...props}
>
  <slot />
</p>
